# dev-build configuration for this repo — read by the dev-build skill
# (skill-library-maint plugin) and scripts/dev-build-check.sh.
# Pattern docs: https://github.com/chzylee/skill-library/tree/main/maintenance/dev-build
skills_root="skills"
stable_branch="master"
dev_branch="dev"
release_steps="update the skill's row/status in the README Skills table if it changed maturity or version"
